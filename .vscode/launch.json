{
    "configurations": [
        {
            "name": "(lldb) Launch",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/stars",
            "args": [],
            "cwd": "${workspaceFolder}",
        },
    ],
    "version": "2.0.0"
}